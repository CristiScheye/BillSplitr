<h1>Summary Table</h1>

<table class="table">
  <tr>
    <th>Friend</th>
    <th>Balance</th>
  </tr>
  <% userBalances.forEach(function(user) { %>
    <tr>
      <th><%= user.escape('email') %></th>
      <th><%= user.escape('balance') %></th>
    </tr>
  <% }) %>

</table>
